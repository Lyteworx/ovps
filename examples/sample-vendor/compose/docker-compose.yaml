# Docker Compose Configuration
# Vendor Package: sample-vendor/demo-app v1.0.0
#
# IMPORTANT: This compose file must work OFFLINE
# - All images must be pre-loaded from containers/v1.0.0/
# - No remote image pulls should be required
# - Use local image references only

version: "3.8"

services:
  # Example service configuration:
  # Uncomment and modify as needed
  #
  # app:
  #   image: sample-vendor/demo-app-app:v1.0.0
  #   container_name: demo-app-app
  #   restart: unless-stopped
  #   ports:
  #     - "8080:80"
  #   volumes:
  #     - ../data/persistent:/app/data
  #   environment:
  #     - APP_ENV=production
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 40s
  #   depends_on:
  #     - db
  #
  # db:
  #   image: sample-vendor/demo-app-db:v1.0.0
  #   container_name: demo-app-db
  #   restart: unless-stopped
  #   volumes:
  #     - ../data/persistent/db:/var/lib/postgresql/data
  #   environment:
  #     - POSTGRES_DB=demo-app
  #     - POSTGRES_USER=demo-app
  #     - POSTGRES_PASSWORD=changeme
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U demo-app"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # Placeholder service - replace with actual services
  placeholder:
    image: alpine:latest
    container_name: demo-app-placeholder
    command: ["echo", "Replace this placeholder with actual services"]

# Networks (optional - Docker creates default network)
# networks:
#   demo-app-network:
#     driver: bridge

# Named volumes (optional - prefer bind mounts to ../data/persistent)
# volumes:
#   demo-app-data:
