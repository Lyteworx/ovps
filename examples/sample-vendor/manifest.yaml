# Vendor Package Manifest
# This is the single source of truth for the package
# Generated by OVPS on 2026-01-20T22:40:04Z

package:
  vendor: "sample-vendor"
  product: "demo-app"
  version: "v1.0.0"
  description: "demo-app by sample-vendor"

# Minimum system requirements
requirements:
  docker_version: "20.10.0"
  disk_space_gb: 10
  memory_gb: 4
  architectures:
    - amd64
    - arm64

# Container images included in this package
containers:
  # List container images with their files
  # - name: "vendor/image-name"
  #   file: "containers/v1.0.0/image-name.tar.gz"
  #   tag: "v1.0.0"

# Services defined in docker-compose.yaml
services:
  # List services that will be started
  # - name: "service-name"
  #   container: "vendor/image-name"
  #   ports:
  #     - "8080:80"

# Data volumes for persistence
volumes:
  # - name: "data-volume"
  #   path: "data/persistent/volume-name"
  #   description: "Persistent storage for service data"

# Health check endpoints
healthchecks:
  # - service: "service-name"
  #   type: "http"
  #   endpoint: "http://localhost:8080/health"
  #   timeout_seconds: 30

# Package metadata
metadata:
  created: "2026-01-20T22:40:04Z"
  generator: "ovps"
  generator_version: "1.0.0"
